# hardshell-telemetry

Read AGENTS.md for the full agent guide: commands, layout, and conventions.
Quick reference: `uv sync` to set up, `uv run pytest` to test,
`uv run ruff check .` to lint, `uv build` to build.
